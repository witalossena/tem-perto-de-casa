import React, { useContext } from 'react';

import shopcontext from '../../../context/ShopContext';
import 


function admin() {
  const { user, handleStore } = useContext(shopcontext)
  return <div />;
}

export default admin;